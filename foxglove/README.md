# Foxglove layout

`airmouse_layout.json` goes here once Role 5 has a panel layout worth exporting (map,
video, `/mission_status`, `/mavros/state`, arm/abort/recall controls). Export it from
Foxglove Studio via Layout → Export layout — don't hand-write it.
