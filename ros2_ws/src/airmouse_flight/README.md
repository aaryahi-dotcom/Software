# airmouse_flight — Role 1: Flight Control & Failsafe

ArduPilot/Pixhawk config, EKF3 without GPS, arm/abort/recall, all failsafes, 5-min boot
sequence. Currently prototyped against ArduPilot SITL via MAVROS (no real hardware).
