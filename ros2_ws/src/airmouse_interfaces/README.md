# airmouse_interfaces

Shared ROS 2 message/service definitions — the topic contract between all five roles.
Change with care: a field type change here breaks the build for every consumer. See
`docs/interfaces.md` for the human-readable contract table.
