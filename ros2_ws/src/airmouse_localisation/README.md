# airmouse_localisation — Role 4a: Localisation

Camera-frame → map-frame transform, grid quantiser, duplicate suppression. Grid cell size
is a config value, not a hardcoded constant — the mission brief never states it.
